# This file is @generated by <https://github.com/liblaf/copier-typescript>.
# DO NOT EDIT!

name: Release

on:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    outputs:
      name: ${{ steps.metadata.outputs.name }}
      version: ${{ steps.metadata.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - id: metadata
        name: Get Package Metadata
        run: |-
          name="$(jq --raw-output '.name' 'package.json')"
          version="$(jq --raw-output '.version' 'package.json')"
          printf 'name=%s\n' "$name" >> "$GITHUB_OUTPUT"
          printf 'version=%s\n' "$version" >> "$GITHUB_OUTPUT"
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install Dependencies
        run: |-
          bun install
          bun pm bin >> "$GITHUB_PATH"
      - name: Build
        run: bun pm pack --destination 'pack'
      - name: Upload Artifacts
        uses: actions/upload-artifact@v5
        with:
          name: pack
          path: pack

  publish:
    name: Publish
    permissions:
      id-token: write
    needs:
      - build
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/${{ needs.build.outputs.name }}/v/${{ needs.build.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          registry-url: https://registry.npmjs.org
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install Dependencies
        run: |-
          bun install
          bun pm bin >> "$GITHUB_PATH"
      - name: Publish
        # TODO: switch to `bun publish` when <https://github.com/oven-sh/bun/issues/15601> is resolved
        run: npm publish --provenance

  release:
    name: Release
    permissions:
      contents: write
    needs:
      - build
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v6
        with:
          name: pack
          path: pack
      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: pack/*
